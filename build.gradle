buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.4.0'
    }
}

// Keep this file very simple. Do not add "allprojects" or "repositories" here.
